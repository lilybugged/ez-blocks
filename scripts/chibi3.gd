extends Spatial

# Declare member variables here. Examples:
# var a = 2
# var b = "text"

# Called when the node enters the scene tree for the first time.
#func _ready():
	#get_tree().get_nodes_in_group("player")[0].get_node("AnimationPlayer").get_animation("mine").set_loop(true)
	#get_tree().get_nodes_in_group("player")[0].get_node("AnimationPlayer").play("mine")

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
